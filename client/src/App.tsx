import React from "react";
import MainPage from "../src/pages/MainPage";

const App: React.FC = () => {
    return (
        <div className="app">
            <MainPage />
        </div>
    );
};

export default App;
